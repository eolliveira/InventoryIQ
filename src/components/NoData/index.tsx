import Typography from '@mui/material/Typography';

export default function NoData() {
  return (
    <Typography margin={2} fontSize={16} fontWeight={'normal'} color={'primary'} variant="h2">
      Não há dados para mostrar.
    </Typography>
  );
}
